name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 16

    - name: Install dependencies
      run: npm ci

    - name: Build
      run: npm run build

    - name: List dist folder contents
      run: ls dist/

    - name: Deploy to EC2
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_PRIVATE_KEY }}
        script: |
          sudo mkdir -p /home/ubuntu/temp-deploy
          sudo chown ${{ secrets.EC2_USERNAME }} /home/ubuntu/temp-deploy
          rsync -avz -e "ssh -i ${{ secrets.EC2_PRIVATE_KEY }}" --delete dist/ ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }}:/home/ubuntu/temp-deploy/
          ls /home/ubuntu/temp-deploy/

    
    - name: Check deployed files
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_PRIVATE_KEY }}
        script: |
          echo "Contents of /home/ubuntu/vue-temp-deploy:"
          ls -la /home/ubuntu/temp-deploy
          echo "Content of the index.html file:"
          cat /home/ubuntu/temp-deploy/dist/index.html
          
    - name: Execute deploy script
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_PRIVATE_KEY }}
        script: |
          ./deploy.sh




